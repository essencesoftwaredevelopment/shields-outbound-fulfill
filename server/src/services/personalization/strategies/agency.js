import { runPersonalization as runEcom } from './ecom.js';

// Placeholder: reuse ecom behavior until a dedicated Agency strategy is defined.
export async function runPersonalization(options) {
    return runEcom(options);
}
